import { AppShell } from "../components/AppShell";

export default function Home() {
  return <AppShell title="Demo"><p className="max-w-2xl text-lg leading-8">A temporal memory and insight layer for tutor builders. Use the navigation to view the same live student graph through three demo lenses.</p></AppShell>;
}
